<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="utf-8" />
        <meta content="width=device-width,initial-scale=1" name="viewport">
        <title>TalkToMe</title>
        <link href="/vendor/bootstrap/css/bootstrap.css" rel="stylesheet">
        <link href="/vendor/bootstrap/css/bootstrap-responsive.css" rel="stylesheet">
        <link href="/css/style.css" rel="stylesheet">
    </head>
    <body>
        <div class="navbar navbar-fixed-top">
            <div class="navbar-inner">
                <div class="container">
                    <a class="brand" href="#">TalkToMe</a>
                </div>
            </div>
        </div>
        <div class="container" id="main">
            <div class="row">
                <div class="span4 offset4">
                    <form class="well" method="post">
                        <div class="control-group">
                            <div class="controls">
                                <% if (error) { %><p>Erreur: <%= error %></p><% } %>
                                <input type="text" name="username" id="username" autofocus required placeholder="Enter your username" <% if (username) { %> value="<%= username %>"<% } %>/>
                            </div>
                            <button type="submit" class="btn">Login</button>
                        </div>
                    </form>
                </div>
            </div>
        </div>

        <script src="http://localhost:1337/socket.io/socket.io.js"></script>
    </body>
</html>